这页展开项目演进的前半部分。第一阶段先做单 NPC 记忆对话原型，是为了验证最小闭环：系统能读取状态、调用 LLM、生成回复，并把过程保存下来。这样项目不是停留在 prompt demo，而是有了 SQLite、workflow 和 trace 作为后续扩展基座。第二阶段加入分层上下文和玩家端，是因为早期原型只看当前输入时角色容易断裂。把 lore、近期上下文、长期记忆和 UI 入口分开后，角色回复更稳定，也更容易调试和展示。
